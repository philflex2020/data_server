port: 4222

jetstream {
  store_dir: "./nats-data"
  max_memory_store: 512MB
  max_file_store: 50GB
  sync_interval: "1s"   # default is 2m — flush to disk before ack to avoid data loss on crash
}

# Basic monitoring endpoint (optional — useful for health checks)
http_port: 8222
