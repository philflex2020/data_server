project_id: 0

mqtt:
  host: localhost
  port: 1883
  topic_prefix: batteries   # publishes to batteries/project=0/site=0/rack=1/module=2/cell=3

hierarchy:
  sites: 3
  racks_per_site: 4
  modules_per_rack: 8
  cells_per_module: 12

rate:
  interval_seconds: 1.0    # publish interval per cell
